{
  "name": "e2e-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "if [ \"$USE_DEV_SERVER\" != \"1\" ]; then cd ../../apps/web && pnpm build; fi",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report playwright-report",
    "install-browsers": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}